import path from "path";

export const COMMENT_HEADING = "<!-- Possible Impact Report -->";

export const MAX_SHOW_FULL_CHANGED_COUNT = 15;
export const MAX_SHOW_DETAIL_CHANGED_COUNT = 10;

export const ROOT_DIR = path.resolve();
